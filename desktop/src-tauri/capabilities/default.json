{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base permissions for the desktop shell, including the verified local-engine start command.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
